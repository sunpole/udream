---
type: feature
project: uDream
series: udream
title: PWA автоматически получает новую версию и предлагает установку
version: 23.8.0
queued_at: 2026-07-20T17:45:27Z
repo_url: https://github.com/sunpole/udream
web_url: https://sunpole.github.io/udream/
image: 2026-07-20-udream-v23-8-0-pwa-update.png
image_source: playwright
image_target: scenario/pwa-install-banner-mobile
image_commit: 5f7af86864134b608402e8cf2dc9db4071071b64
image_captured_at: 2026-07-22T09:47:22Z
publication_repair: unpublished-invalid-image
publication_repair_reason: replace a non-PNG asset rejected by Telegram before the first publication
---

# uDream 23.8.0: обновление PWA и установка с сайта

Исправлена ситуация, при которой сайт и установленное приложение могли продолжать показывать старую версию из кэша, хотя на GitHub Pages уже была опубликована новая версия.

- Service Worker теперь сразу активирует обновление через `skipWaiting()` и `clients.claim()`.
- Старые кэши удаляются только внутри семейства `udream-*`, не затрагивая другие проекты на том же домене.
- HTML, JavaScript, манифест и JSON получают стратегию «сначала сеть, затем кэш».
- Добавлен независимый `version.json`, который запрашивается без браузерного кэша.
- При обнаружении несовпадения версий приложение запрашивает обновление и выполняет защищённую однократную перезагрузку.
- При переходе со старого Service Worker открытая страница переводится на актуальную версию.
- На сайте появляется заметный баннер установки PWA.
- Баннер скрывается внутри уже установленного приложения.
- Кнопка «Установить» вызывает системное окно браузера, когда оно доступно.
- Для неподдерживаемых браузеров доступна ручная инструкция установки.
- Маленький крестик скрывает баннер только до следующей перезагрузки страницы.
- Обновлены версия приложения, package metadata и PWA manifest.
- Добавлены регрессионные проверки регистрации, запрета HTTP-кэша и немедленной активации Service Worker.

Активная база из 4 086 записей и тексты словаря не изменялись.

Изображение к публикации — настоящий мобильный Chromium-снимок версии `v23.8.0`: на нём видны баннер установки и раскрытая ручная инструкция. Снимок создан из точного Git commit после автоматической проверки версии и текста баннера.

Публикационное исправление выполнено до первого Telegram-поста: прежний файл имел расширение `.png`, но не PNG-сигнатуру, поэтому Telegram корректно отклонил его с `IMAGE_PROCESS_FAILED`. Текст и порядок очереди сохранены, дублирующая публикация не создаётся.

Короткий текст для Telegram:

Обновление uDream 23.8.0 исправляет зависание сайта и установленной PWA на старой версии. Приложение теперь проверяет актуальную версию без кэша, сразу активирует новый Service Worker и предлагает установку заметным баннером с крестиком до следующей перезагрузки страницы.
