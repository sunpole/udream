# WORK_STATUS — передача работы между устройствами

Этот файл — единая оперативная точка продолжения разработки uDream с телефона, рабочего ПК, MacBook или другого устройства.

Он отвечает на пять вопросов:

1. Начата ли сейчас работа?
2. Какая цель и в какой ветке выполняется?
3. Что уже сделано фактически?
4. Где именно остановились?
5. Какой следующий точный шаг?

`ROADMAP.md` хранит общий план проекта, `docs/PROJECT_STATE.md` — проверенное состояние продукта, а этот файл хранит **живую точку передачи текущей работы**.

## Быстрый сигнал

| Поле | Текущее значение |
|---|---|
| Состояние | **IN_PROGRESS** — реализация 23.8.6 завершена, выполняется проверка Pull Request |
| Рабочая ветка | `ops/unified-ai-workflow-v23.8.6` |
| Открытый Pull Request | `#21` — `docs: unify AI GitHub workflow and screenshot evidence` |
| Стабильный релиз | `v23.8.0` |
| Документационный baseline | кандидат `v23.8.6` — единая работа ИИ ↔ GitHub и доказательства скриншотов |
| Последнее завершённое изменение в `main` | commit `ac7dfe6b49567d29b0d994f04a3c9d315a7aaf5f` |
| Активная задача | проверить PR #21 и исправить все замечания GitHub Actions |
| Следующая утверждённая работа после merge | `23.8.7` — Playwright desktop/mobile screenshots и workflow artifacts; затем D1.1 |

## Текущая точка продолжения

Статус: `IN_PROGRESS`

Начато: `2026-07-22 09:14 Europe/Berlin`

Устройство/среда: `ChatGPT + GitHub connector`

Ветка: `ops/unified-ai-workflow-v23.8.6`

Pull Request: `#21`

Цель: создать единую систему, в которой GitHub остаётся источником истины независимо от количества чатов, устройств и ИИ-агентов; подготовить следующий отдельный этап автоматических реальных скриншотов для патчноутов и uNews.

Планировалось:

- устранить оставшиеся расхождения между `WORK_STATUS.md`, Issue №19, `VERSION.md` и `docs/PROJECT_STATE.md`;
- закрепить точный порядок источников истины и запрет продолжения по памяти старого чата;
- добавить единый стартовый протокол для любого ИИ/устройства;
- усилить шаблон Pull Request и автоматический валидатор handoff-состояния;
- документировать требования к реальным скриншотам и отдельный этап Playwright-автоматизации;
- сохранить D1.1 следующим продуктовым этапом без изменения активной базы.

Планируемые файлы:

- `WORK_STATUS.md`
- `AGENTS.md`
- `VERSION.md`
- `CHANGELOG.md`
- `README.md`
- `ROADMAP.md`
- `docs/PROJECT_STATE.md`
- `docs/AI_GITHUB_WORKFLOW.md`
- `docs/SCREENSHOT_AUTOMATION.md`
- `docs/NEWS_PUBLISHING.md`
- `docs/FILE_MAP.md`
- `.github/pull_request_template.md`
- `scripts/validate-project.mjs`
- `scripts/validate-patchnote-diff.mjs`
- uNews-патчноут, SVG-источник и новый реальный PNG

Критерии завершения:

- любой новый чат понимает состояние, прочитав GitHub без контекста старых сообщений;
- конфликтующие или устаревшие указания автоматически выявляются;
- активная задача, ветка и PR согласованы между `WORK_STATUS.md` и GitHub;
- новый PR содержит фактический handoff-блок;
- реальный скриншот не может быть заменён старым нерелевантным изображением;
- тесты, валидатор и GitHub Actions проходят;
- после merge состояние возвращено в `READY`, а следующим шагом становится `23.8.7`, затем D1.1.

Уже сделано:

- проверены текущий `main`, документация, Issue №19, PR №20 и последние commits;
- Issue №19 обновлён и теперь является техническим заданием D1.1, а не оперативным handoff;
- создана и опубликована ветка `ops/unified-ai-workflow-v23.8.6`;
- добавлен единый протокол `docs/AI_GITHUB_WORKFLOW.md`;
- добавлена спецификация `docs/SCREENSHOT_AUTOMATION.md`;
- усилены `AGENTS.md`, PR template и оба валидатора;
- новые патчноуты обязаны добавлять новое изображение и screenshot provenance;
- GitHub Actions создал новый PNG из фактического SVG-источника этой ветки;
- добавлен патчноут `23.8.6` с новым изображением;
- восстановлена полная прежняя история `CHANGELOG.md` после обнаружения случайного обрезания;
- открыт Pull Request #21.

Последний проверенный commit:

- `d39ce94aa00331df2640239cc447c58adb4e91b0` — полный changelog восстановлен перед обновлением handoff.

Следующий точный шаг:

- проверить список файлов и GitHub Actions PR #21; при ошибке исправить ветку, при успехе подготовить merge и возврат `WORK_STATUS.md` в `READY`.

Что нельзя делать при продолжении:

- не создавать параллельную ветку для 23.8.6;
- не начинать 23.8.7 или D1.1 до завершения PR #21;
- не менять runtime, PWA или активную базу;
- не переиспользовать старое изображение;
- не объединять PR при неуспешной GitHub Actions проверке.

## Обязательный порядок перед началом работы

На любом устройстве или у любого нового агента:

```bash
git switch main
git pull --ff-only origin main
```

Затем обязательно:

1. прочитать `WORK_STATUS.md`;
2. проверить открытые Pull Request и последние коммиты;
3. при `IN_PROGRESS`, `PAUSED` или `BLOCKED` продолжать только указанную ветку и задачу, не создавать конкурирующую реализацию;
4. при `READY` создать отдельную ветку для следующей утверждённой задачи;
5. обновить раздел **Текущая точка продолжения**;
6. зафиксировать цель, планируемые файлы и критерии завершения;
7. только после этого изменять проект.

## Как зафиксировать паузу

Если работу нельзя закончить на текущем устройстве, нельзя оставлять только фразу «продолжить позже». Нужно записать:

```text
Статус: PAUSED или BLOCKED
Что фактически сделано:
- ...

Что не сделано:
- ...

Почему остановились:
- ...

Последний commit:
- ...

Незакоммиченные изменения:
- нет | точный список

Следующий точный шаг:
- конкретное действие или команда

Что нельзя делать при продолжении:
- ...
```

При паузе изменения этого файла должны быть закоммичены и отправлены в GitHub. Локальная незапушенная запись не является передачей работы между устройствами.

## Как завершить задачу

Перед завершением обновить файл фактическими данными:

```text
Статус: COMPLETED
Планировалось:
- ...

Сделано фактически:
- ...

Не вошло в задачу:
- ...

Изменённые файлы:
- ...

Проверки:
- команда — результат

Pull Request / merge:
- ...

Релиз / версия:
- ...

Оставшиеся риски:
- ...

Следующая утверждённая работа:
- ...
```

После merge вернуть верхний быстрый сигнал в состояние `READY`, указать последний завершённый PR/commit и следующий утверждённый шаг.

## Значения статуса

- `READY` — незавершённой задачи нет, можно начинать следующую утверждённую работу.
- `IN_PROGRESS` — задача начата и продолжается в указанной ветке.
- `PAUSED` — работа сохранена в GitHub и может быть продолжена с другого устройства.
- `BLOCKED` — продолжение невозможно до устранения явно записанного препятствия.
- `COMPLETED` — реализация закончена, но перед возвратом к `READY` должны быть записаны проверки, PR/merge и следующий шаг.

## Главные правила

- Нельзя начинать новую крупную задачу, если здесь записана незавершённая `IN_PROGRESS`, `PAUSED` или `BLOCKED` работа, пока не принято явное решение продолжить или закрыть её.
- Нельзя объявлять запланированное выполненным без diff, commit или проверки.
- Нельзя оставлять точку продолжения только в чате, на одном устройстве или в локальном терминале.
- Старый чат, память ИИ и локальная ветка без push не являются источником истины.
- Pull Request подробно объясняет конкретный patch, но `WORK_STATUS.md` показывает общую текущую точку проекта.
- При конфликте этого файла с GitHub фактами приоритет имеют `main`, открытый Pull Request и реальные commits; конфликт нужно сразу исправить в этом файле.
